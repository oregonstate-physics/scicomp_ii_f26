# Scientific Computing II — direct dependencies.
#
# This is the authoritative list. environment.yml and .devcontainer/ both install
# FROM this file, so there is one set of packages, not three that drift apart.
#
# Direct dependencies only: pip resolves the rest. The previous version of this
# file was a `pip freeze` dump, which meant transitive packages (and two that are
# no longer maintained) were pinned here as if the course used them.

# --- core ---
numpy
scipy
pandas
matplotlib
seaborn

# --- astronomy ---
astropy
astroML
corner
daft                    # drawing graphical models (week 8)

# --- Bayesian inference ---
numpyro
jax
arviz

# --- machine learning ---
scikit-learn
flax
optax
clu                     # training loops for the Flax notebooks (weeks 8-9)
ml-collections
tensorflow
tensorflow-datasets

# --- signal processing ---
gwpy                    # LIGO strain data (week 10); was missing, so
                        # week-10/03-notch-filters-ligo.ipynb could not run
pydub                   # audio rendering (week 10); needs ffmpeg on the system

# --- notebook environment ---
jupyterlab
ipykernel
ipywidgets
tqdm

# Not installed by default, used only by notebooks/extras/ and
# homework/extras/: pycbc, healpy. Install them by hand if you work through
# those.
